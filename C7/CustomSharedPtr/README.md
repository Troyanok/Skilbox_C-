# Реализация умного указателя

Собственная реализация умного указателя `shared_ptr_toy` с подсчётом ссылок для объектов `Toy`.

## Сборка
```bash
mkdir build && cd build
cmake ..
make
./custom_shared_ptr
